# Local Ops Workspace

This folder is reserved for machine-local operational artifacts that should stay outside Git history.

Current uses:

- `backups/passreserve/`
  - compressed Passreserve database snapshots
  - metadata manifests
  - run logs

The backup archives themselves are intentionally ignored by Git.
